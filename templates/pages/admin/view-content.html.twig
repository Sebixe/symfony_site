{% extends 'layouts/main.html.twig' %}

{% block title %}Voir le contenu{% endblock title %}
{% block content %}

        <div class="container">
            <div class="about-us-content">
                <div class="gallery-jobs">
                    <div class="row">
                     <div class="col-md-12 col-sm-6">
                         <div class="single-package-item">
                    <img src="{{ asset('assets/images/packages/view.jpg') }}" alt="package-place">
                    <div class="single-package-item-txt">
                        <h3>Gérer le contenu</h3>
                        <div class="row">
                            </br>

                            <table class="table">
                                <thead><tr><th>Id</th><th>Titre</th><th>Adresse</th><th>Extension</th><th>Type</th><th>Niveau</th><th>Edit</th></tr></thead>
                                <tbody>
                                <div class="text-center">
                                    <a href="{{ path('createcontent')}}">
                                        <button type="button" class="btn btn-info mb-2" > Ajouter un contenu </button>
                                    </a>
                                </div>
                                {% for content in contents %}
                                <tr>
                                    <td>{{ content.id }}</td>
                                    <td>{{ content.title }}</td>
                                    <td class="disapear">{{ content.adresse }}</td>
                                    <td>{{ content.extension }}</td>
                                    <td>{{ content.type }}</td>
                                    <td>{{ content.level }}</td>
                                    <td>
                                        <a href="{{ path('editcontent', {id: content.id})}}"><button class="btn btn-primary"><i class="fa fa-edit"></i></button></a>
                                    </td>
                                </tr>
                                {% endfor %}
                                </tbody>
                            </table>
                            <div class="text-center">
                            <a href="{{ path('createcontent')}}">
                                <button type="button" class="btn btn-info mb-2" > Ajouter un contenu </button>
                            </a>
                            </div>
                        </div>


                    </div>
                </div>
            </div>
        </div>
                    </div>
            </div>
        </div>
    </section>

{% endblock content %}

